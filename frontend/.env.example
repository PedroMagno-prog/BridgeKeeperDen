# URL base da API backend em producao (ex: Render)
# Em desenvolvimento local, deixe vazio para usar o proxy do Vite (/api/v1)
# Em producao no Vercel, configure como a URL do seu backend no Render:
VITE_API_URL=https://bridgekeeperden.onrender.com/api/v1
