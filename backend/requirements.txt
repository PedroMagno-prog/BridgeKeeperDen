# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy>=2.0.47,<2.1
sqlalchemy-cockroachdb==2.0.4
alembic==1.14.0
asyncpg==0.30.0          # async PostgreSQL driver (asyncpg)
psycopg2-binary==2.9.10  # sync PostgreSQL driver (Alembic offline mode)
greenlet==3.5.4           # required by SQLAlchemy async bridge (Alembic migrations)

# Settings & Environment
pydantic==2.10.0
pydantic-settings==2.6.0
email-validator==2.2.0     # required for pydantic EmailStr
python-dotenv==1.0.1

# Auth & Security
# ✅ bcrypt — hash de senha. Mantido ativamente; sem dependências legadas.
#    Substitui passlib[bcrypt] (passlib está abandonada desde 2023).
bcrypt==4.2.1
# ✅ PyJWT — geração e validação de JWT. Padrão da indústria, atualizado.
#    Substitui python-jose (vulnerabilidades e falta de manutenção).
PyJWT==2.9.0
python-multipart==0.0.12

# HTTP Client
httpx==0.28.0

# Dev tools
pytest==8.3.0
pytest-asyncio==0.24.0

# Markdown & Frontmatter
python-frontmatter==1.1.0

# Image Processing
Pillow==10.4.0
